query SomeDoc($address: String!) {
  events(input: { address: $address, from: 0, to: 1000 }) {
    blockInfo {
      chainStatus
      timestamp
      stateHash
      globalSlotSinceHardfork
    }
  }
}
